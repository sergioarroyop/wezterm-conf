local wezterm = require("wezterm")

local styling_config = {}

-- Fonts and colors
styling_config.font = wezterm.font("JetBrains Mono")
styling_config.font_size = 12.0
styling_config.color_scheme = "GruvboxDark"

return styling_config
